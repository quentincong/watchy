# Watchy — Codex entry point

Before doing any work in this repository, read `CLAUDE.md` in full and follow it.
`CLAUDE.md` is the authoritative shared project instruction file for both Codex and
Claude Code. Resolve its relative paths from the repository root.
